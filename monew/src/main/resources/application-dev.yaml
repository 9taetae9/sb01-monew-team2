spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/monew
    username: monew
    password: monew1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
#  data:
#    mongodb:
#      uri: mongodb+srv://${MONGO_USERNAME}:${MONGO_PASSWORD}@${MONGO_HOST}/${MONGO_DB_DEV}?retryWrites=true&w=majority&appName=Cluster0
  boot:
    admin:
      client:
        url: http://localhost:9090
logging:
  level:
    org.mongodb.driver.protocol.command: DEBUG
