spring:
  jpa:
    hibernate:
      ddl-auto: none

  batch:
    jdbc:
      initialize-schema: always
  sql:
    init:
      mode: never
      schema-locations:
        - classpath:schema.sql
aws:
  credentials:
    accessKey: tmp
    secretKey: tmp
  profile-name: monew-acc
  region: tmp
  s3:
    bucket-name: tmp
