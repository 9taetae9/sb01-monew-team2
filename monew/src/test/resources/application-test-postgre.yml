spring:
  jpa:
    hibernate:
      ddl-auto: update

  batch:
    jdbc:
      initialize-schema: always

aws:
  credentials:
    accessKey: tmp
    secretKey: tmp
  profile-name: monew-acc
  region: tmp
  s3:
    bucket-name: tmp
